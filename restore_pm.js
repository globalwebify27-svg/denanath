const fs = require('fs'); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const backup = JSON.parse(fs.readFileSync('db_backup.json', 'utf8')); const depts = backup.Department || backup.departments || []; const pm = depts.find(d => d.id === 'cmpxpxqn70001p31m129au27o'); if (pm) { await prisma.department.update({ where: { id: pm.id }, data: { description: pm.description } }); console.log('Restored PM'); } else { console.log('PM not found in backup'); } } main().finally(() => prisma.$disconnect());
