const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const setting = await prisma.setting.findUnique({ where: { key: 'page_simulation_other_facilities' } }); console.log(JSON.parse(setting.value).content); } main().finally(() => prisma.$disconnect());
