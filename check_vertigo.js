const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); prisma.department.findFirst({ where: { name: { contains: 'VERTIGO' } } }).then(d => require('fs').writeFileSync('vertigo_current.html', d ? d.description : 'Not found')).finally(() => prisma.$disconnect());
